const path = require('path');

module.exports = {
  'config': path.resolve('src/config', 'database.js'), // Using .js for dynamic env loading
  'models-path': path.resolve('src/models'),
  'seeders-path': path.resolve('src/db/seeders'), // Optional: if using seeders
  'migrations-path': path.resolve('src/db/migrations')
};
